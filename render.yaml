services:
  - type: web
    name: doe-backend
    env: python
    rootDir: DOE-1/backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PORT
        value: 8000
